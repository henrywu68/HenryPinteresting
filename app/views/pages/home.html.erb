<div class="jumbotron center">
	<h1>Welcome to my App!</h1>
	<% if user_signed_in? %>
	 歡迎登入會員
	<% else %>
		<p>
			<%= link_to "註冊" , new_user_registration_path, class: "btn btn-default btn-lg" %>
			<%= link_to "登入" , new_user_session_path, class: "btn btn-danger btn-lg" %>
		</p>
	<% end %>
</div>